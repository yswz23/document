根据题目要求尝试编写一个完整的程序。编程语言请使用java/C#/C++/Python等面向对象语言。
本题目以考察编程素养为目的，请积极使用搜索引擎查阅资料，各个模块的实装方式都可以参考或拷贝网上的优秀解决策略。
出于时间限制，可以不用完成整个程序，请重点注意程序结构、最佳实践(best practice)和代码风格上。
比起一个完成形的杂乱程序，结构清晰的未完成程序将得到更高的评价。
----------------------------------------------------------------------------------------------------------
１．编写一个简易"数据库"程序。
主要功能：通过[标准输入]输入命令，可以存储、查询以及删除数据。命令执行结果从[标准输出]输出。
         数据存储在本地文件系统中，格式任意，不需要加密。数据字段格式均为字符串，每个字段最大长度限制固定为100个字符。
         不需要有用户管理（唯一一个用户），但需要登陆密码，可以编辑、但不能删除密码。初始密码固定为"password"。
程序形式：控制台程序（命令行执行）
实行环境：Windows或Linux的任意版本（只需支持一种环境即可）
具体功能：
         命令结束符为";"。命令中的单词/关键词以空格分割，一个或多个空格均看作一个分隔符。需要有基本的语法检查功能。
         关键词（参考下列命令一览）：CREATE DELETE INSERT INTO SELECT FROM WHERE AND TABLE VALUES ( ) , = ！=
         需要实现的命令：
           1.创建[表]
             语法：CREATE TABLE 表名 ( 字段名 [, 字段名 ... ] )
             例：CREATE TABLE TEST_TABLE ( Test1, Test2, Test3 )
           2.删除[表]
             语法：DELETE TABLE 表名
             例：DELETE TABLE TEST_TABLE
           3.插入数据
             语法：INSERT INTO 表名 VALUES ( 值 [, 值 ... ] )
             例：INSERT INTO TEST_TABLE VALUES ( abc, 123, abc123 )
           4.查询数据(条件语法只需支持 等于[=]、不等于[!=]、并且[AND])
             语法：SELECT 字段名 [, 字段名 ... ] FROM 表名 WHERE 字段名 [ = | != ] 值 [AND 字段名 [ = | != ] 值 ... ]
             例：SELECT Test1, Test2 FROM TEST_TABLE WHERE Test3 = abc123
           5.删除数据
             语法：DELETE FROM 表名 WHERE 字段名 [ = | != ] 值 [AND 字段名 [ = | != ] 值 ... ]
             例：DELETE FROM TEST_TABLE WHERE Test3 = abc123 AND Test2 != abc
----------------------------------------------------------------------------------------------------------
２．

