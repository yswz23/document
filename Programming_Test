使用伪代码或任意编程语言写出满足题目要求的程序段落。使用伪代码可以不用遵循任何语法标准，易于理解即可。使用特定编程语言请注明是哪种语言。
题目中的input/output为标准输入/输出。

------------------------------------------------------------------------------------------------------
例题：实现IndexOf，输出字符串S2在字符串S1中首次出现的位置。位置从0开始计算，如果S1不包含S2，则输出-1。
input: 固定为4行。第1行是S1的内容，第2行是S1的长度，第3行是S2的内容，第4行是S2的长度。S1,S2仅含数字和字母。
output: 字符串S2在字符串S1中首次出现的位置

Sample Input 1:
abcdefg
7
de
2
Sample Output 1:
3

Sample Input 2:
abc123
6
1c
2
Sample Output 2:
-1

Sample Input 3:
abc123
6
abc1234
7
Sample Output 3:
-1

解答例：（伪代码）
BEGIN IndexOf
  read S1，length1，S2，length2
  IF length1 < length2 THEN
    print -1
    return
  ENDIF
  FOR i ← 0 to (length1 - length2) DO
    contains ← true
    FOR j ← 0 to (length2 - 1) DO
      IF S1[i + j] <> S2[j] THEN
        contains ← false
        break
      ENDIF
    DONE
    IF contains = true THEN
      print i
      return
    ENDIF
  DONE
  print -1
END IndexOf

------------------------------------------------------------------------------------------------------
１．你有一个培养皿和若干个细胞A的原体。每个细胞A在培养皿中经过24小时将分裂成2个细胞A。
如果只能每天0点往培养皿中放入一个细胞A，求你需要多少个细胞A的原体，才能恰好培育出N个细胞A。
input: 正整数N（1 <= N <= 0x0FFFFFFF）
output：需要的细胞A数量

Sample Input 1:
8
Sample Output 1:
1
注：放入1个细胞，一天后分裂为2个，两天后分裂为4个，三天后分裂为8个。所以需要1个细胞原体。

Sample Input 2:
7
Sample Output 2:
3
注：放入1个细胞，一天后分裂为2个。再放入1个细胞，此时有3个细胞，两天后分裂为6个，再放入1个细胞达到7个细胞。一共放入了3个细胞。
------------------------------------------------------------------------------------------------------
２．
------------------------------------------------------------------------------------------------------
３．
------------------------------------------------------------------------------------------------------
４．
------------------------------------------------------------------------------------------------------
５．
------------------------------------------------------------------------------------------------------
６．
------------------------------------------------------------------------------------------------------
７．
