使用伪代码或任意编程语言写出满足题目要求的程序段落。使用伪代码可以不用遵循任何语法标准，易于理解即可。使用特定编程语言请注明是哪种语言。
题目中的input/output为标准输入/输出。

------------------------------------------------------------------------------------------------------
例题：实现IndexOf，输出字符串S2在字符串S1中首次出现的位置。位置从0开始计算，如果S1不包含S2，则输出-1。
input: 固定为4行。第1行是S1的长度，第2行是S1的内容，第3行是S2的长度，第4行是S2的内容。S1,S2仅含数字和字母。
output: 字符串S2在字符串S1中首次出现的位置

Sample Input 1:
7
abcdefg
2
de
Sample Output 1:
3

Sample Input 2:
6
abc123
2
1c
Sample Output 2:
-1

Sample Input 3:
6
abc123
7
abc1234
Sample Output 3:
-1

解答例：（伪代码）
BEGIN IndexOf
  read length1，S1，length2，S2
  IF length1 < length2 THEN
    print -1
    return
  ENDIF
  FOR i ← 0 to (length1 - length2) DO
    contains ← true
    FOR j ← 0 to (length2 - 1) DO
      IF S1[i + j] <> S2[j] THEN
        contains ← false
        break
      ENDIF
    DONE
    IF contains = true THEN
      print i
      return
    ENDIF
  DONE
  print -1
END IndexOf

------------------------------------------------------------------------------------------------------
１．你有一个培养皿和若干个细胞A的原体。每个细胞A在培养皿中经过24小时将分裂成2个细胞A。
如果只能每天0点往培养皿中放入一个细胞A，求你需要多少个细胞A的原体，才能恰好培育出N个细胞A。
input: 正整数N（1 <= N <= 0x0FFFFFFF）
output：需要的细胞A数量

Sample Input 1:
8
Sample Output 1:
1
注：放入1个细胞，一天后分裂为2个，两天后分裂为4个，三天后分裂为8个。所以需要1个细胞原体。

Sample Input 2:
7
Sample Output 2:
3
注：放入1个细胞，一天后分裂为2个。再放入1个细胞，此时有3个细胞，两天后分裂为6个，再放入1个细胞达到7个细胞。一共放入了3个细胞。
------------------------------------------------------------------------------------------------------
２．单项链表是一种简单的数据存储结构，它由一个头部节点（head）和若干链表节点(node)组成。
它的每一个节点包含2个部分：该节点的信息（值）以及指向下一个节点的指针。最后一个节点指向下一个节点的指针为NULL。
由此可以从头部节点线性地遍历每一个节点。
本题目要求将给出的数据(integer)依次存入一个单项链表中，之后将该单向链表反转，再依次输出。
input: 第1行是数据的个数N，第2到N+1行是需要存入单项链表中的数据。
output：反转后的数据依次输出

Sample Input 1:
5
2
3
1
3
4
Sample Output 1:
4
3
1
3
2
------------------------------------------------------------------------------------------------------
３．在空间直角坐标系中有一个球体和一个各边都与坐标轴平行的正方体，已知球体的球心坐标和半径，正方体的中心坐标和边长。求算球体和正方体是否有重合部分。
xyz轴的范围都为[-10000， 10000]，只用考虑整数运算。
input: 第1行是用空格分开的球心的x，y，z坐标。第2行是球体的半径。第3行是用空格分开的正方体中心的x，y，z坐标，第4行是正方体的边长。
output：有重合部分则输出true，没有则输出false。

Sample Input 1:
1 1 1
1
2 3 4
8
Sample Output 1:
true

Sample Input 2:
1 2 3
3
10 10 20
1
Sample Output 2:
false
------------------------------------------------------------------------------------------------------
４．某个商店里的所有商品单价均为N元，每购买1件商品可以获得1点积分，使用P点积分可以购买1个商品，用积分购买的商品仍可获得积分。积分和现金不能共用。
求用M元最多能购买到多少件商品。M，N，P均为大于1的正整数。
input: M,N,P以空格分隔
output：最多能购买到的商品数。

Sample Input 1:
22 3 2
13
注:使用21元购买7件商品获得7点积分，使用6点积分购买3件商品获得3点积分，此时还剩4点积分。
   再使用4点积分购买2件商品获得2点积分，再使用2点积分购买1件商品,最终剩下1元和1点积分。共购买了13件商品。




------------------------------------------------------------------------------------------------------
５．
------------------------------------------------------------------------------------------------------
６．
------------------------------------------------------------------------------------------------------
７．
