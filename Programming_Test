使用伪代码或任意编程语言写出满足题目要求的程序段落。使用伪代码可以不用遵循任何语法标准，易于理解即可。使用特定编程语言请注明是哪种语言。
题目中的input/output为标准输入/输出。

------------------------------------------------------------------------------------------------------
例题：实现IndexOf，输出字符串S2在字符串S1中首次出现的位置。位置从0开始计算，如果S1不包含S2，则输出-1。
input: 固定为4行。第1行是S1的内容，第2行是S1的长度，第3行是S2的内容，第4行是S2的长度。S1,S2仅含数字和字母。
output: 字符串S2在字符串S1中首次出现的位置

Sample Input 1:
abcdefg
7
de
2
Sample Output 1:
3

Sample Input 2:
abc123
6
1c
2
Sample Output 2:
-1

Sample Input 3:
abc123
6
abc1234
7
Sample Output 3:
-1

解答例：（伪代码）
BEGIN IndexOf
  read S1，length1，S2，length2
  IF length1 < length2 THEN
    print -1
    return
  ENDIF
  FOR i ← 0 to (length1 - length2) DO
    contains ← true
    FOR j ← 0 to (length2 - 1) DO
      IF S1[i + j] <> S2[j] THEN
        contains ← false
        break
      ENDIF
    DONE
    IF contains = true THEN
      print i
      return
    ENDIF
  DONE
  print -1
END IndexOf

------------------------------------------------------------------------------------------------------
１．
------------------------------------------------------------------------------------------------------
２．
------------------------------------------------------------------------------------------------------
３．
------------------------------------------------------------------------------------------------------
４．
------------------------------------------------------------------------------------------------------
５．
------------------------------------------------------------------------------------------------------
６．
------------------------------------------------------------------------------------------------------
７．
